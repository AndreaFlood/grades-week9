function grade(){
        var subjects= prompt ("enter number of subjects?");
        console.log(subjects);

        while(subjects>0){
                subjects=subjects-1;
               grades= prompt("enter grade for subject");
                console.log(grades);   
         }
}